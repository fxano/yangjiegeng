<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="demo.com">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".OwnInfoSkip" />
        <activity android:name=".OwnRelease" />
        <activity android:name=".OwnCollection" />
        <activity android:name=".OwnComment" />
        <activity android:name=".OwnBrowse" />
        <activity android:name=".MyFollow" />
        <activity android:name=".MyFans" />
        <activity android:name=".FollowMain" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".SettingSafety" />
        <activity android:name=".SettingAbout" />
        <activity android:name=".SettingHelp" />
        <activity
            android:name=".WelcomeActivity"
            android:theme="@style/AppWelcome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>